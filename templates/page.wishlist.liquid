{% include 'breadcrumbs' %}
<div class="page-wishlist mb-40">
    {%- if customer -%}
        {% include 'wishlist' %}
    {%- else -%}
        <script>
            window.location.href = {{ routes.account_login_url | json }};
        </script>
    {%- endif -%}
</div>
