{% include 'breadcrumbs' %}
<div class="page-compare mb-40">
    {%- if customer -%}
        {% include 'compare' %}
    {%- else -%}
        <script>
            window.location.href = {{ routes.account_login_url | json }};
        </script>
    {%- endif -%}
</div>
